<h3>注文履歴一覧</h3>

<table class="table-secondary">
  <thead>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </thead>
<%= @order.each do |order| %>
  <tbody>
    <tr><%= order.created_at%></tr>
    <tr><%= order.name%></tr>
    <tr><%= order.order_details.all.quantity%></tr>
    <tr><%=order.status %></tr>
  </tbody>

　　　<%=@order.name%>


<% end %>
</table>


